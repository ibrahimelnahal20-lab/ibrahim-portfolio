/// hover_glow.dart
/// Effect to create glowing border/shadow on hover over items.
library;

import 'package:flutter/material.dart';

class HoverGlow extends StatelessWidget {
  const HoverGlow({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
